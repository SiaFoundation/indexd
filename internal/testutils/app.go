package testutils

// App is a helper type for testing that can upload data to hosts, download data
// from hosts and pin data to an indexer. The only state it maintains is the
// state that an actual app would maintain which means
type App struct {
}
